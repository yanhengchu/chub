# Copy this file to .env and replace the value with a long random token.
HUB_TOKEN=replace-with-a-long-random-token

# Optional: use a node-specific configuration file.
# HUB_CONFIG_FILE=/absolute/path/to/settings.yaml
