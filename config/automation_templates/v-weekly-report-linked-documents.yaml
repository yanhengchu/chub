version: 1
type: feishu-linked-documents

source:
  section: "各端周报"
  link_type: markdown
  allowed_paths:
    - /wiki/
    - /docx/
  max_documents: 20
  required_current_documents: 5
  # 日期型旧周报链接仅用于从本期输入中排除，不再作为参考资料下载或校验。
  background_reference_title_kind: period_week

download:
  template: feishu-document-download
  format: markdown
  continue_on_error: true
