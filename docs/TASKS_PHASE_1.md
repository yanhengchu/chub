# Hub 第一阶段任务清单

## 1. 使用说明

本文档用于跟踪第一阶段开发任务。任务按依赖顺序排列，每个里程碑完成后应通过对应自动化测试，并保持应用可运行。

状态约定：

- `[ ]` 未开始
- `[x]` 已完成

## 2. M1：基础工程与健康检查

- [x] 建立 Python 3.12+ 工程和依赖声明。
- [x] 建立 Core、API、Services、Tasks、Web 和 Tests 目录。
- [x] 提供不含真实凭证的节点示例配置。
- [x] 实现 YAML 配置读取和 Pydantic 校验。
- [x] 支持从环境变量读取访问 Token 和配置路径。
- [x] 实现 macOS、Ubuntu 和未知平台检测。
- [x] 实现控制台及文件日志初始化。
- [x] 创建 FastAPI 应用和启动入口。
- [x] 实现无需认证的健康检查接口。
- [x] 补充配置、平台和健康检查测试。
- [x] 更新 README，说明环境准备、配置和启动方式。

验收：应用可启动，健康检查返回服务状态和版本；未设置 Token 时给出警告但不阻止健康检查，自动化测试通过，真实 Token 和运行日志未进入 Git。

## 3. M2：认证与节点状态

- [ ] 实现 Bearer Token 认证。
- [ ] 确保请求与错误日志不记录 Token。
- [ ] 实现节点和系统状态采集服务。
- [ ] 实现受保护的状态接口。
- [ ] 统一成功与错误响应结构。
- [ ] 补充认证、状态采集和状态接口测试。

验收：有效 Token 可读取完整状态，无效或缺失 Token 被拒绝，健康检查仍可公开访问。

## 4. M3：白名单任务

- [ ] 定义任务和任务结果模型。
- [ ] 实现任务注册表和平台过滤。
- [ ] 实现任务执行器、异常处理和超时边界。
- [ ] 实现公共任务 `show_version`。
- [ ] 实现公共任务 `check_system`。
- [ ] 实现 macOS 任务 `check_codex`。
- [ ] 实现 Ubuntu 任务 `check_docker`。
- [ ] 实现受保护的任务列表接口。
- [ ] 实现受保护的任务执行接口。
- [ ] 拒绝未注册任务、不支持平台及非法参数。
- [ ] 补充注册、执行、平台和任务接口测试。

验收：每个平台只展示适用任务，任务结果结构一致，失败和超时不影响节点服务，不存在任意命令入口。

## 5. M4：日志查看

- [ ] 实现安全的 Hub 日志尾部读取。
- [ ] 限制日志行数范围。
- [ ] 实现受保护的日志接口。
- [ ] 验证日志不包含访问 Token。
- [ ] 补充日志读取和接口安全测试。

验收：用户只能读取配置确定的 Hub 日志，不能传入文件路径，返回行数符合限制。

## 6. M5：Web 管理页面

- [ ] 建立移动端优先的页面骨架和样式。
- [ ] 实现 Token 录入、可选保存、更换和清除。
- [ ] 使用受控 API 展示节点状态。
- [ ] 展示当前平台可用任务。
- [ ] 展示任务执行中、成功、失败和超时状态。
- [ ] 提供近期日志查看入口。
- [ ] 处理无效凭证和接口不可用状态。
- [ ] 完成浏览器基本交互测试。

验收：用户可通过手机浏览器完成 PRD 定义的主要操作，Token 不被明文回显。

## 7. M6：双平台验收

- [ ] 在 macOS 使用正式节点配置启动并完成自动化测试。
- [ ] 验证 macOS Codex 检查结果。
- [ ] 在 Ubuntu 使用同一版本代码启动并完成自动化测试。
- [ ] 验证 Ubuntu Docker 检查结果。
- [ ] 通过可信网络从手机分别访问两个节点。
- [ ] 验证任一节点离线不影响另一节点。
- [ ] 记录第一阶段验证结果和遗留问题。

验收：PRD 第 9 节所有标准得到验证，并形成是否进入下一阶段的结论。

## 8. 当前任务

M1 已完成。下一项为 M2：认证与节点状态；开始前先 review 当前本地改动。
