# Hub 第一阶段验收记录

> 归档状态：M1–M7 已验收通过，第一阶段已正式关闭。本文档冻结为最终验收记录。

## 1. 验收结论

第一阶段功能、自动化测试、macOS 与 Ubuntu 实机检查、手机双节点访问及断机
隔离检查均已通过。两个节点能够使用同一版本代码独立运行，并分别提供受控的
状态、任务和日志能力。

最终结论：**第一阶段全部验收通过，正式关闭。**

## 2. 自动化与静态验证

M1–M6 于 2026-07-19 在 Ubuntu 节点使用当时的 `main` 工作树完成基线验证：

- `python -m pytest`：当时共 82 项，全部通过。
- `python -m pip check`：依赖完整，无冲突。
- Python 源码编译：通过。
- `app/web/static/app.js` JavaScript 语法检查：通过。
- Git 空白与补丁格式检查：通过。

自动化测试覆盖配置、平台检测、健康检查、认证、状态、任务注册与执行、命令
超时、日志读取、API 错误结构、页面资源和安全响应头。加入 M7 服务管理测试后，
第一阶段最终完整测试为 98 项，全部通过。

## 3. Ubuntu 实机结果

- 节点身份：`Ubuntu`。
- 实际平台：`ubuntu`。
- 主机名：`chuyh-box`。
- 状态采集：CPU、内存和磁盘指标正常返回。
- Docker 客户端：29.6.1。
- Docker Compose：5.3.0。
- Docker Server：29.6.1。
- Docker 容器概况：正常返回。
- Ubuntu M1–M6 基线自动化测试：82 项通过；最终完整测试为 98 项，
  全部通过。

## 4. macOS 实机结果

- 使用正式 macOS 节点配置成功启动。
- 平台正确识别为 `macos`。
- 自动化测试通过。
- Codex 检查正常返回可用状态和版本。
- 手机可访问管理页面，并正常查看状态、执行任务和读取日志。

## 5. 双节点与隔离结果

- 手机通过可信网络可分别访问 MacBook 与 Ubuntu 节点。
- 任一节点停止时，另一节点的页面、状态、任务和日志不受影响。
- 两个节点使用各自独立的 Token 和 `settings.local.yaml`。
- 恢复节点后可重新正常访问，不依赖另一节点的运行状态。

## 6. 遗留与后续优化

- 第一阶段仍以可信局域网或虚拟组网为部署边界，不直接暴露公网。
- 浏览器端目前以源码约束和人工手机验收为主；后续可增加 Playwright 端到端测试。
- 依赖使用兼容版本范围；后续正式发布可增加跨平台锁定文件。
- 应用版本同时存在于包元数据和节点配置；下一阶段可收敛为单一版本来源。
- 独立发布目录、自动升级和回滚流程留待下一阶段设计。

## 7. M7 服务化运行验收

自动化验证覆盖 macOS LaunchAgent 与 Ubuntu systemd user service 配置生成、
重复安装、已有命令保护、配置日志路径和安全卸载。当前完整测试共 98 项，
全部通过。

macOS 于 2026-07-20 使用当前开发工作区完成实机验证：

- LaunchAgent 安装、配置格式和运行状态正常。
- `start`、`stop`、`restart`、`status` 和 `logs` 命令正常。
- 强制结束 Python 进程后服务能够自动恢复。
- 重启后的健康检查及后台 Codex 检查通过。
- 重复安装可更新服务；卸载后源码、本机配置和日志保持不变。

实机验收过程中发现并修复了相对路径安装产生错误命令链接，以及停止操作触发
LaunchAgent 立即恢复的两个问题，并增加了回归测试。

Ubuntu 于 2026-07-20 使用当前 `main` 工作区完成实机验证：

- systemd user service 安装、登录自启配置和运行状态正常。
- 可从其他目录使用 `chub` 命令，`start`、`stop`、`restart`、`status` 和
  `logs` 均正常。
- 后台健康检查、节点状态和 Docker 检查正常；Docker 客户端、Compose 和
  Server 版本均可读取。
- 强制结束 Python 进程后，systemd 能够自动恢复服务，恢复后健康检查正常。
- 重复安装可安全更新服务。
- 卸载后 systemd unit 和命令链接已移除，源码、`.env`、本机配置和日志均
  保持不变。

至此 M7 双平台服务实机验收完成，第一阶段正式关闭。
