# AGENTS.md

## 项目定位

Chub 是个人维护的轻量设备管理服务，主要技术栈为 Python 3.12、FastAPI、Pydantic、Jinja2、原生 JavaScript/CSS 和 Shell。支持 macOS LaunchAgent 与 Ubuntu systemd user service。

项目维护者主要负责提出需求、讨论方案和验收实际运行结果；编码 Agent 负责在后台阅读与修改代码、完成必要文档同步和基本验证。协作以功能是否符合预期为主，不要求维护者参与代码实现细节。

## Agent 首要理解规则

- 本项目的文档第一读者是 AI Agent，第二读者是项目维护者。Agent 阅读或修改文档前，必须先明确 Chub/目标功能是什么、本文负责什么、哪些内容不在范围内；关键规则、状态所有权、失败边界、恢复方式和验收标准必须写成可执行的明确规则，不能只保留背景介绍或实现过程。
- Agent 处理需求时先阅读项目 README、总体架构和目标专项文档，再检查代码与测试；不得只根据单个函数、页面提示或历史讨论推断项目规则。文档冲突时遵循项目 README 规定的权威层级，并同步修正过时引用。
- 文档主要服务于理解和正确执行，不为维护者堆积重复说明。实现细节只有在影响安全、数据、兼容性、恢复或验收时才进入正文；维护者操作步骤放在规则之后。
- 设计新流程时以功能可用、可恢复和最终状态可确认作为优先目标。不要因为追求形式上的完整状态或极端并发场景，增加没有明确业务收益的复杂门禁。

## 核心不可违反规则

AI Agent 处理任何需求前先用下面的规则建立判断基线，详细边界再按后文和专项文档展开：

1. **先理解再修改**：先读 README、总体架构和目标专项文档，确认功能目标、权威状态、范围和验收标准；不能只根据页面文案、单个函数或旧讨论推断行为。
2. **安全边界不可放宽**：认证、固定命令/路径白名单、敏感信息保护、真实微信操作限制和 OpenClaw 固定路由必须失败关闭；客户端不能扩大权限。
3. **状态以最终结果为准**：每类状态只有一个权威来源；同一逻辑 Session 同时只能有一个 writer。进程创建、HTTP 200、任务已受理或 Tool Call 已创建都不等于成功，必须确认业务终态、健康状态或通知终态。
4. **旧运行态默认不兼容**：功能或协议变化时，Chub 自有且不再适用的旧运行数据默认按固定边界丢弃并初始化新版本；不为过渡状态增加双读、双写或长期迁移分支。用户配置、第三方原生数据和明确要求保留的数据不适用此默认规则。
5. **门禁最小且可恢复**：只阻止当前直接冲突或破坏风险，按资源局部生效；不得把忙/未知状态扩散成全局不可用。对执行层能够直接判断可行性的操作，`unknown` 不是拒绝结论，应先调用对应的 native、Worker 或终端执行层，由执行层返回成功、失败或状态未知；只有执行层无法安全尝试，或结果无法确认会造成直接风险时，才失败关闭。空闲或可安全接管的 Session 应允许切换/恢复；临时拒绝必须有原因、解除条件和恢复路径，不能永久锁死。
6. **异步操作可用优先**：普通 Web、Worker、ClawBot 重启彼此独立；系统升级与恢复是例外，只锁定直接受影响的 AI Runtime 写入及服务。无关的只读能力、独立服务和可用入口继续工作。新增门禁必须说明业务必要性和验收场景。
7. **保持改动可控**：优先小步复用现有结构；不做无关重构，不引入没有真实需求的框架、队列或兼容层；发现文档与实现冲突时先指出并同步权威文档。

## 协作方式

- 优先直接分析和解决问题；沟通简短，只在需要确认方向、存在风险或执行较久时说明关键进展。
- 需求只有小范围空白时，可沿用现有行为做合理假设，并在结果中说明。
- 改动保持小步、聚焦，优先复用现有结构、代码风格、响应格式和交互，不顺手重构无关内容或引入复杂架构。
- 发现更大结构问题时先简要说明，但继续聚焦当前任务。
- 用户只要求分析、评审或解释时，不擅自修改代码。
- 功能验收以实际页面、交互和运行状态为主；完成改动后主动进行最小充分验证，并检查相关文档和跨平台影响。
- 涉及真实微信场景验收时，只能由维护者本人在微信客户端执行发送文字、发送语音、点击和查看收件结果。编码 Agent 必须先提供简短验收步骤并请维护者操作，严禁通过 Computer Use、AppleScript、辅助功能、坐标点击或其他自动化方式读取、操作或控制本机微信；Agent 仅可检查 Chub/OpenClaw 的后台日志、任务状态和通知终态，并结合维护者反馈判断结果。后台记录不能替代维护者对微信实际收发结果的确认。
- 最终回复不粘贴实现代码或要求维护者逐文件检查，只说明结果、关键验证、重点验收项及必要风险。

## 结果输出规范

项目维护者主要负责需求、设计和实际验收，不需要人工 review 大量实现代码。功能开发完成后的最终回复应面向产品验收，优先说明：

- 完成了什么功能，用户现在可以看到或执行什么。
- 页面、交互和状态发生了什么变化。
- 做了哪些验证，验证结果是否通过。
- 维护者应如何验收，给出简短、可操作的步骤。
- 是否存在风险、限制、未验证平台，以及是否需要重启或重新安装。

默认不要展开讲解具体类、函数、CSS 选择器或实现过程，不粘贴实现代码、diff 或大段测试输出，不罗列大量修改文件，也不要求维护者逐文件 review。只有实现细节会影响安全、兼容性、数据、后续维护，或维护者明确要求代码评审时，才补充必要的技术说明。

分析、评审类任务不套用功能交付格式，直接给出结论、影响和建议。

## 项目结构

- `app/api/`：FastAPI 接口。
- `app/ai_runtime/`：Runtime Adapter、Runner 契约和运行时边界。
- `app/ai_session/`：逻辑 AI Session、Activity、入口和 Session Manager。
- `app/ai_usage/`：Codex/OpenAI 额度与用量采集及共享快照。
- `app/core/`：配置、安全、日志、平台检测等基础能力。
- `app/notifications/`：通知模型、投递和终态记录。
- `app/requests/`：轻量需求储备和请求命令。
- `app/services/`：可复用服务逻辑。
- `app/tasks/`：白名单维护任务及平台实现。
- `app/codex/`：Codex PTY 会话、票据和连接管理。
- `app/quick_worker*.py`：Quick Worker 服务入口、任务、Runner 和本机协议。
- `app/automations/`：配置驱动的浏览器自动化、Runner、状态和跨进程锁。
- `app/web/templates/`：Jinja2 页面模板。
- `app/web/static/`：原生 JavaScript 和 CSS。
- `docs/`：项目说明、总体架构、专项设计、能力契约和阶段归档。
- `integrations/openclaw/chub/`：Chub OpenClaw 插件源码、构建产物、测试和部署说明。
- `scripts/`：安装、服务管理和辅助脚本。
- `tests/`：pytest 测试。
- `config/`：跨平台配置示例；本机配置不提交。

## 实现约束

- API 保持统一的 `ApiResponse`/`ApiError` 响应结构。
- 在 Chub 的 Codex PTY 或快速交互 Codex 模式中发送飞书通知时，直接使用 `chub notification send` 或 Chub 通知 API，不要通过 `openclaw agent` 间接调用；OpenClaw TUI 和微信入口才使用 `chub_send_notification`。
- 受保护接口只接受真实 loopback socket，或在默认启用的 `security.allow_tailscale` 未被关闭时接受真实 Tailscale socket 来源；其他来源拒绝。不得信任客户端转发 Header，健康检查除外。
- 不允许客户端提供任意文件路径或任意系统命令，只能使用后端固定映射或白名单。
- 不在日志、响应、测试输出或文档中暴露 Token、Authorization、终端票据等敏感数据。
- 项目资料列表和设计文档详情是可信网络内的只读页面，不要求认证；文档内容不得包含凭证、账号信息、本机秘密或其他不适合直接访问的内容。首页项目资料卡片的动态刷新接口仍需真实 loopback 或未被关闭的 Tailnet 可信访问。
- 页面渲染外部内容和日志时使用 `textContent`，不要使用 `innerHTML`。
- 文件读取必须限制行数、字节数和单行长度，避免一次加载无界内容。
- 后台服务行为需同时考虑 macOS 和 Ubuntu；无法实机验证的平台要在结果中说明。
- Web 自重启使用 `scripts/chub-web-restart`，不要从服务内部执行 `bootout` 后再依赖同一子进程启动服务。
- 快速交互中需要重启 Chub 时，只调用一次 `scripts/chub-web-restart` 登记任务级 Web 重启请求，不直接调用
  `launchctl`、`systemctl` 或其他服务管理命令；继续完成任务和最终回复。Chub 只等待请求任务自身结果与完成通知，
  随后直接执行 Web 重启，不等待其他 Session、快速任务或翻译。
- 异步操作不能仅凭子进程成功创建就宣告完成；重启等操作应通过实例 ID、健康状态或其他最终状态确认结果。
- 业务门禁必须遵循“最小必要、局部生效、可恢复”：只阻止会直接冲突或造成破坏的当前操作，不把一个资源的忙/未知状态扩散成全局不可用，也不阻塞无关入口和维护能力。门禁判断必须基于当前真实执行状态和实际 owner，不能只根据历史 writer 标记、旧 PID、过期锁或页面状态长期阻塞。状态未知时，先判断具体执行层是否能够安全尝试并给出结果；能尝试的操作不得由 Chub 仅凭 `unknown` 提前拒绝，执行层失败或结果未知时再按该操作的保留和重试规则收敛。
- 等待输入、空闲或可安全接管的 Session 应允许按产品规则切换 writer 或执行恢复；确需拒绝时必须给出对象、原因、恢复路径和有限重试边界。未知状态只在确有数据破坏或安全风险时暂时失败关闭，并必须有清理、对账或恢复路径，不能形成永久锁死。
- 升级、恢复和服务维护默认只锁定直接受影响的 AI Runtime 写入及相关服务操作；不要用全局门禁阻断 ClawBot、只读状态、无关自动化或其他独立服务。新增门禁前必须说明必要风险、影响范围、解除条件和验收场景。
- 新增维护操作时，同时考虑操作日志的 `requested`、`started`、`succeeded` 和 `failed` 状态；无法由旧进程确认最终结果时，不要伪造成功日志。
- 微信或 OpenClaw 触发 Chub 操作时，必须能够以非敏感标识关联入口请求、允许身份、OpenClaw 会话、Chub 操作 ID、目标节点和最终结果；不把模型回复或 Tool Call 已创建当作操作成功。微信 Chub 模式的高权限提交入口只接受同机 OpenClaw 的真实 loopback socket；只读状态可由本机或固定 Tailnet 读取，跨节点提交必须另行设计调用方身份边界。
- 外部 Agent 首先只调用固定只读或低风险白名单能力。重复消息、超时、断链和最终回复失败必须受控处理；高风险能力需要独立设计并逐次明确确认，不因 Owner、Tailnet 认证或模型判断自动放行。唯一已批准的例外是“微信 Chub 模式”：同机 OpenClaw 上当前绑定的单一微信 Owner 通过微信通道使用当前 Session 时，可使用该模式配置的 `Full access` 直接执行；授权随受控绑定关系生效，不是 Session 的永久属性，也不适用于其他 Agent、身份或入口，撤销时关闭微信 Chub 模式。
- 微信 ClawBot 的设备能力调用固定为“微信 ClawBot → OpenClaw → Chub → OpenClaw → 微信 ClawBot”。Chub 不得为处理该类请求调用 OpenClaw Agent、Gateway Agent 路由或 `openclaw agent`。受控例外仅包括：提交微信 Chub 任务前读取本机通道状态以校验本次 Hook 路由；任务结束后使用该任务保存的账号和发送者调用 `openclaw message send` 发送结果，超长结果按固定上限编号分段且不得无限发送，任务请求协调 Web 重启时在新实例健康确认后按同一路由发送独立重启结果；页面来源快速交互使用全局固定接收人发送完成通知，但不发送重启结果，账号默认选择唯一健康 ClawBot，可由兼容性配置明确覆盖。以上均不得触发新的设备能力，微信任务路由缺失或失效时不得回退到全局目标。Chub 只读复用 OpenClaw 的模型配置、直连模型供应商，以及直接调用自身通知能力均不属于反向调用。
- 微信语音只有通道提供的可信来源标记才能进入语音路径；不得根据正文猜测或接受客户端自行声明，任务正文始终使用干净转写。文本优化的状态、恢复和幂等边界以 `docs/OPENCLAW_CUSTOMIZATION_DESIGN.md` 与 `docs/CHUB_QUICK_WORKER_DESIGN.md` 为准，用户可见行为和通知格式以能力清单第 4 节为准。实现必须保持失败关闭：优化失败不得执行原文、切换目标或重复提交已经受理的派生任务。
- 微信 Chub 固定指令以 `docs/CHUB_INTEGRATION_CAPABILITIES.md` 第 4 节为唯一产品契约，`AGENTS.md` 不重复维护语法和回复格式。固定指令及其通知的固定文案默认使用英文，只有能力清单明确登记的 `help` 双语例外；调整指令时同步解析、双语帮助、业务测试和能力清单。只有身份、路由、并发、持久化或通知边界变化时才修改接入设计，涉及插件调度协议时再执行插件协议升级清单。
- 轻量需求储备使用 R1–R9 九个活动槽位，不创建专用需求 Session。只有维护者明确要求“保存需求”时，Agent 才把已经讨论成型的小需求整理为标题、目标、必要背景、约束和验收标准，并通过 `chub request save --title <标题>` 的标准输入写入；只保存，不开始实现，也不生成独立设计文档。明确要求“更新需求 RN”时，先用 `chub request show RN`读取现有内容，再通过 `chub request update RN [--title <标题>]` 的标准输入整体更新；不得直接编辑需求状态文件。活动需求满九个、正文超过 2000 字符或内容已经需要独立方案时停止保存并说明原因。普通讨论、分析或评审不写入需求池；保存或更新后只回报槽位、标题和状态。
- Chub 功能涉及 OpenClaw 插件变更时，必须先在本仓库 `integrations/openclaw/chub/` 同步维护源码、静态清单、测试和插件说明，并在对应设计文档记录行为、配置、部署及验收变化；OpenClaw 实际加载目录只接收由本仓库构建并验证的运行产物，不得直接修改后形成仓库外的独立实现。
- 微信统一调度协议升级时，以 `integrations/openclaw/chub/README.md`“协议升级同步清单”为维护入口，配套更新 Chub、插件、测试、构建产物、部署状态和必要文档，避免只修改单侧版本号。
- 不随意引入前端框架、数据库、任务队列或其他复杂基础设施。

## 前端目录与加载边界

- 首页 JavaScript 按 `static/js/core/`、`static/js/components/`、`static/js/features/` 和 `static/app.js` 分层；`app.js` 只负责页面组合、跨功能协调和启动，新功能的请求、状态与渲染放入对应 Feature 文件。
- 当前首轮拆分仍使用全局函数和固定 `defer` 顺序，不是 ES Modules。`index.html` 中的脚本顺序属于兼容契约，调整时必须同步顺序测试并完成浏览器回归。
- 首页共享请求、认证、页面恢复和跨功能清理由 Core 承担；通用 DOM 行为放入 Components；Feature 之间不直接读取对方内部状态。
- 公共页面样式通过 `templates/partials/app_styles.html` 统一加载，顺序固定为 `tokens.css`、`base.css`、`components.css`、`responsive.css`；不要恢复已删除的 `static/app.css`，也不要在各模板重复维护样式列表。
- CSS 首轮按原规则顺序安全拆分，`components.css` 仍可能包含页面专用规则。后续只在真实页面改动时渐进归类，不为了目录整齐批量搬动导致层叠变化。

## UI 风格基线

- 当前默认 UI 风格命名为 `Standard`，中文可称“简约标准版”。它代表当前已经验收的轻量工具型界面，不代表功能受限的基础版本。
- Standard 以清晰的信息层级、克制的颜色与装饰、自然高度卡片、统一间距和轻量动效为主要特征；现有首页卡片、弹窗、原生控件和折叠规范均属于这套基线。
- 后续新增页面和新增功能默认遵循 Standard，优先复用现有样式 Token、公共组件和交互规范；只有明确业务需求无法由现有模式表达时才新增样式，并同步完成桌面端与手机端验收。
- 新风格应保持业务功能、信息结构、接口、安全边界、键盘操作和响应式可用性一致，优先通过样式 Token 与公共组件实现，不复制 Feature 业务逻辑。
- 新风格至少与 Standard 对比信息密度、色彩层次、字体、圆角阴影、间距、动效、桌面/手机布局和无障碍表现；主题选择与偏好持久化需有明确需求后单独设计和验收。
- `Cyber` 是已验收并可在设置页启用的科技终端风格。它与 Standard 共用信息结构和组件语义，以深黑底色、冷青绿状态色、轻切角半透明面板、命令提示符、等宽元信息和低干扰代码雨形成差异；正文仍使用无衬线字体。风格选择和代码雨速度/亮度/密度保存在当前浏览器，读取失败时回退 Standard；风格选择另同步非敏感主题 Cookie，供服务端首屏直接设置风格和 `color-scheme`，不得为此放宽 CSP。代码雨只作背景、不得拦截交互，减少动态效果时必须停止流动并避免同列雨串重叠。
- Standard 与 Cyber 的普通文字按钮和按钮型导航统一使用 14px 字号并复用相同主次语义。Cyber 只为普通操作按钮和按钮型导航增加 `>`；页面标题、Session 主体、图标按钮、弹窗关闭/取消、置顶及正文链接按语义排除。快速交互发送按钮复用次级按钮并保持单行；Cyber 状态标签需保持文字、背景与非纯颜色的状态指示。

## Web UI 交互规范

- 重要功能优先使用职责单一的独立卡片；每张动态卡片独立请求、缓存、刷新、轮询、渲染和报错，单卡失败不得清空、阻塞或闪烁其他卡片。
- 刷新时保留最近一次成功内容，成功后原位更新，失败时保留旧内容并单独提示；页面恢复时优先使用安全的会话级缓存。
- 卡片“刷新”只刷新本卡片；卡内操作只更新本卡片。全局或共享资源操作只定向刷新真实受影响的卡片。
- 标题区最多放一个操作；筛选等辅助操作放独立工具栏，“全部任务/文档”等次级入口放卡片底部右侧。
- 首页、次级列表及下钻默认使用当前标签并依赖浏览器返回；下级页面不增加专用返回按钮。卡片默认不因返回刷新，确需刷新时只更新带返回刷新标记的卡片；即使浏览器因 `no-store` 重建首页，也应识别历史返回并优先恢复其他卡片的会话缓存，不能把连接初始化误当成全部卡片刷新。
- 桌面双列和手机单列均使用卡片自然高度，不因同排卡片拉伸；卡片简介建议约 12–18 个字符，以准确和视觉平衡为先。
- 业务确认、选择和编辑使用 `showModal()` 模态 `dialog`，不得使用浏览器默认 `confirm()` 或 `alert()` 承载业务交互。确认操作统一复用公共弹窗的遮罩、表面、标题、影响说明、反馈区和操作区；桌面居中，手机按内容使用居中或底部抽屉。
- 归档、停止、退出和其他危险操作必须写明具体对象、直接影响及恢复边界，并使用危险确认按钮；恢复等非危险确认沿用次级按钮。弹窗空闲时支持关闭按钮、取消、遮罩和 Escape，提交期间禁用关闭与操作并设置忙碌状态。
- 同步提交失败保留弹窗并在反馈区展示原因；异步操作一旦已被后端受理，可以关闭弹窗并在所属卡片或全局区域持续展示进度和最终失败，不把“已受理”当作成功。
- 原生表单控件继续使用系统原生弹层。全局禁用默认 tap-highlight，只保留明确设计的组件反馈。
- 即时生效的开关、下拉框和滑杆由控件本身表达当前状态；读取或保存成功后不重复显示“已开启”“已保存”等常驻成功提示，保存期间可临时禁用控件。
- 设置项只在失败，或存在控件无法表达且会影响用户判断的重要状态时显示补充反馈；状态恢复后清除。作用范围、生效规则和风险放在设置项说明中。

### 卡片折叠

- 可折叠卡片只保留标题层和内容层两个直接子容器；业务内容、状态、操作和相关弹窗均放入内容层，内容层用 `gap` 管理同级间距，子元素不重复保留外边距。
- 标题横条统一控制折叠；展开时点击标题折叠，折叠后点击卡片展开。独立按钮、链接、表单控件和内容区操作不得误触发折叠。
- 标题支持键盘操作和 `aria-expanded`。折叠开始立即阻止内容交互，结束后再隐藏；展开时先恢复内容。折叠只影响展示，不改变数据、轮询或业务状态。
- 折叠偏好保存在浏览器本地；无历史时使用默认值，存储不可用时退回页面内存状态。首次渲染和恢复状态不播放动画。
- 折叠时内容约 `140ms` 淡出，再用约 `180ms` 收起高度；内容不得随高度向上滑动，标题与内容间距同步收起。展开时高度、间距和淡入同步恢复。
- 系统启用“减少动态效果”时直接切换；动态内容刷新不触发折叠动画。

## 日志规范

- 操作日志用于记录用户触发的维护行为；运行日志用于排障，两者不要混为单一语义。
- 操作日志至少包含时间、操作 ID、动作、状态、目标和来源 IP，不宣称具备用户身份审计能力；可选 `reason` 只能记录已脱敏、受长度限制的固定诊断，不得透传原始异常、命令输出或用户正文。
- 操作成功和失败都应记录；日志记录本身不得改变原操作的响应语义。
- 日志来源必须由后端固定枚举映射，禁止客户端传入任意路径。
- 页面读取必须限制行数、读取字节数和单行长度；分页遇到超长单行时仍必须保证游标向前推进。
- 页面展示可以截断超长行；日志下载应保留完整内容并进行敏感信息脱敏，同时设置合理的文件大小上限。
- 日志文件及轮转文件默认使用 `600` 权限，不依赖系统默认 umask 提供安全性。
- 日志轮转、文件增长和服务重启期间允许安全降级，但不能导致无限分页、任意文件读取或服务不可用。

## 配置与本机文件

- `config/settings.local.yaml` 只保存本机配置，不应提交。
- `config/automations.local.yaml` 保存本机真实自动化任务，不应提交；示例维护在 `config/automations.example.yaml`。
- 不覆盖用户现有配置；新增配置项应提供安全默认值。项目一般不为旧运行数据、旧协议状态或已改变功能保留兼容读取、双写或迁移逻辑；功能变更时，默认直接放弃 Chub 自有且不再适用的旧数据，使用固定后端映射或白名单清理并初始化新版本，清理后必须验证新实例最终状态。只有维护者明确要求保留、数据不可重建或存在外部契约时，才设计最小迁移/兼容方案，并同步记录范围、失败处理、清理边界和验收方法。
- 上述规则不授权删除第三方原生数据、用户配置或不可逆业务数据；这类数据必须有明确需求、固定白名单和恢复边界。配置文件继续保持不覆盖，旧运行态与旧协议兼容则默认不做，避免为过渡状态长期增加复杂分支。
- 不把真实 Token、IP、主机名或用户目录写入示例配置和测试夹具。
- 服务直接依赖当前工作区和 `.venv`，涉及服务定义变化时说明是否需要重新执行安装。
- 设计文档的目录、状态、索引和归档规则以 README“项目资料维护”为准；只在功能说明、结构或任务状态明显变化时同步相关文档，不把“已实现”直接视为归档条件。项目资料页的“隐藏”只控制首页展示，不等同于仓库文档归档。
- 当前专项设计统一使用以下最低文档结构：头部依次写 `状态`、`主要读者`、`本文负责` 和 `本文不负责`；`状态` 必须使用并精确匹配 `docs/design_documents.json` 的标准值，具体维护触发条件另写为补充说明，不把“已实现”“已验收”混写成新的索引状态。文档正文按“AI 可执行契约、当前行为与边界、维护者操作”组织，跨领域规则只引用唯一权威文档，不复制完整正文。
- 已验收的当前专项文档末尾必须保留简短的“验收范围与复检”说明，至少明确已验证的功能/平台、未验证或不承诺的范围，以及会触发重新验收的接口、状态所有权、协议、外部依赖或用户可见契约变化。局部实机验证不能表述为全平台或全链路验收；没有实际验证的平台必须明确标注。
- 查询或引用 Chub 当前可用插件、插件能力、固定 API 和消息路由时，以 `docs/CHUB_INTEGRATION_CAPABILITIES.md` 为统一入口；微信固定指令的语法、用户可见行为和回复格式以该文档第 4 节为准。其中标记为“已实现”“已接入”或“已验收”的条目可视为当前可用。

## 验证

根据改动范围执行最小充分验证；交付前至少运行相关测试：

```bash
.venv/bin/python -m pytest tests/test_relevant_file.py
```

公共配置、API、中间件或跨模块改动运行全量测试：

```bash
.venv/bin/python -m pytest
```

- Shell 脚本运行 `bash -n scripts/<script-name>`；Web 交互或布局变化尽量做真实浏览器回归。
- 异步服务操作必须验证最终状态，而非只看 HTTP 200：确认实例或 PID、健康状态、操作日志和失败展示一致；不要为验证意外中断用户正在使用的服务。
- 文件、日志或分页至少覆盖不存在/空/读取失败、超长行、非 UTF-8、敏感信息截断边界、非法来源或参数、权限、大小上限和轮转。
- 测试使用临时目录和测试配置，不写入真实日志、本机配置或用户目录；替身不得执行真实服务管理、删除或重启。
- 交付前从维护者视角确认：成功提示代表最终状态；认证、统一响应和安全头未退化；不存在任意命令/路径、无界读取或敏感信息泄漏；配置未被覆盖；不必要的旧运行态兼容、全局门禁和无关改动没有被引入；跨平台差异已处理或说明。
- 最后运行 `git diff --check` 和 `git status --short`。

## Git、文档与交付

- 默认不创建分支，不提交、推送或创建 PR；先由维护者验收本地结果。
- 本机因 AI API 访问限制必须保持 VPN 开启，不以关闭 VPN 作为 GitHub 排障手段。VPN 环境下 GitHub 网页或 `git ls-remote` 等附加远端探测短时超时属于已知网络特性，不据此判定远端或同步失败；代码同步以 `git fetch`、`git pull`、`git push` 的最终结果为准，仅在这些命令持续失败时排查网络。
- 保留工作区其他改动，不处理、回退或提交无关文件；若用户要求提交，只包含当前任务相关文件并先确认 diff。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令清理用户改动。
- 小改动不要求先改文档；功能说明、结构、配置或任务状态明显变化时，优先同步 README、架构说明和任务清单，确保命令、路径和接口与代码一致。
- 最终回复简短说明：完成效果、关键验证、重点验收项、风险或未验证平台，以及是否需要重启、重装或其他额外操作。
